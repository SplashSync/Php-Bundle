<?php

$finder = PhpCsFixer\Finder::create()
    ->exclude('tests/Fixtures')
    ->exclude('vendor')
    ->exclude('var')
    ->in(dirname(dirname(dirname(dirname(dirname(dirname(__DIR__)))))))
;

include_once "php_cs.rules.dist";

return $config;