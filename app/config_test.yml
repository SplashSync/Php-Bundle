imports:
    - { resource: config/parameters.yml }
    - { resource: packages/framework.yml }
    - { resource: packages/doctrine.yml }
    - { resource: packages/splash.yml }

framework:
    router:
        resource:   "%kernel.root_dir%/routing_test.yml"
    session:
        storage_id: session.storage.mock_file
