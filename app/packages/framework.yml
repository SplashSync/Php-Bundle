# Framework Configuration
framework:
    translator:      { fallbacks: ["%locale%"] }
    router:
        resource:   "%kernel.build_dir%/routing.yml"
    form:           ~
    session:        ~
    profiler: { only_exceptions: false }

